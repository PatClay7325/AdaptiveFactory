import { memo } from 'react';

/**
 * Executive Summary Component
 * Keep it minimal to avoid dependencies on context providers
 */
function ExecutiveSummary() {
    return (
        <div>
            {/* Content will be added back once routing is working */}
        </div>
    );
}

export default memo(ExecutiveSummary);